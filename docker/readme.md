- per eseguire il container: docker compose up -d --build
- il contenuto di 'vinyl vault' sarà visibile su localhost:8080
- per phpMyAdmin localhost:8081
- le pw:    MYSQL_ROOT_PASSWORD: rootpassword
            MYSQL_DATABASE: tecweb_db
            MYSQL_USER: tecweb_user
            MYSQL_PASSWORD: tecweb_password